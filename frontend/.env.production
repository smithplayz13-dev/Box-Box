# Production defaults — override via Vercel Environment Variables for your deployment
# Do not commit real secrets here; Vercel env vars take precedence over this file at build time
VITE_API_URL=https://pitwall-ai-q77t.onrender.com
VITE_API_KEY=fallback_dev_key
